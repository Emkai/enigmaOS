-- Programs used elsewhere (autostart.lua, binds.lua)
local M = {
    terminal    = "kitty",
    fileManager = "dolphin",
    menu        = "wofi -M fuzzy --show drun -i",
    browser     = "chromium",
    music       = "spotify",
}

return M
