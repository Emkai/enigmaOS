#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=src/dictate.sh
source "$SCRIPT_DIR/src/dictate.sh"

dictate_main "$SCRIPT_DIR" "$@"
